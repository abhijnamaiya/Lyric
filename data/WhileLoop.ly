start

num x;
str s;
bool b;

play x 0;
play s "testprint";

bool play y yeah;

check ((5* 10) == (30 + 30 - 10)) here {
  loop (x < 4) {
    release s;
    play x (x + 1);
  }
} there {
  release "Notestprinthere";
}

stop